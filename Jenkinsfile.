// Declarative Pipeline
pipeline {
    agent any 

    stages {
        stage('Checkout Code') {
            steps {
                echo 'Cloning repository...'
            }
        }
        stage('Build') {
            steps {
                echo 'Starting the build process...'
                echo 'Build successful!'
            }
        }
    }
}
